angular.module('apapnow').controller('bookedCtrl',function($scope,loginSrv){



  //this function should recieve USERNAME and PASSWORD and search if DB already has that
  console.log('booked', loginSrv.user)


})
